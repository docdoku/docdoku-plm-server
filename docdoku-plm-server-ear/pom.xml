<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.docdoku.plm</groupId>
        <artifactId>docdoku-plm-server</artifactId>
        <version>2.6.0-SNAPSHOT</version>
    </parent>
    <artifactId>docdoku-plm-server-ear</artifactId>
    <packaging>ear</packaging>
    <name>docdoku-plm-server-ear Java EE 8 Assembly</name>
    <properties>
        <context-root></context-root>
    </properties>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-ear-plugin</artifactId>
                <configuration>
                    <version>7</version>
                    <defaultLibBundleDir>/lib</defaultLibBundleDir>
                    <generateApplicationXml>true</generateApplicationXml>
                    <modules>
                        <ejbModule>
                            <groupId>com.docdoku.plm</groupId>
                            <artifactId>docdoku-plm-server-config</artifactId>
                            <bundleFileName>docdoku-plm-server-config.jar</bundleFileName>
                        </ejbModule>
                        <ejbModule>
                            <groupId>com.docdoku.plm</groupId>
                            <artifactId>docdoku-plm-server-attributes-importer</artifactId>
                            <bundleFileName>docdoku-plm-server-attributes-importer.jar</bundleFileName>
                        </ejbModule>
                        <ejbModule>
                            <groupId>com.docdoku.plm</groupId>
                            <artifactId>docdoku-plm-server-ejb</artifactId>
                            <bundleFileName>docdoku-plm-server-ejb.jar</bundleFileName>
                        </ejbModule>
                        <webModule>
                            <groupId>com.docdoku.plm</groupId>
                            <artifactId>docdoku-plm-server-rest</artifactId>
                            <contextRoot>${context-root}</contextRoot>
                            <bundleFileName>docdoku-plm-server-rest.war</bundleFileName>
                        </webModule>
                    </modules>

                </configuration>
            </plugin>
        </plugins>
        <finalName>${project.artifactId}</finalName>
    </build>
    <dependencies>
        <dependency>
            <groupId>com.docdoku.plm</groupId>
            <artifactId>docdoku-plm-server-config</artifactId>
            <version>${project.version}</version>
            <type>ejb</type>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>com.docdoku.plm</groupId>
            <artifactId>docdoku-plm-server-attributes-importer</artifactId>
            <version>${project.version}</version>
            <type>ejb</type>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>com.docdoku.plm</groupId>
            <artifactId>docdoku-plm-server-ejb</artifactId>
            <version>${project.version}</version>
            <type>ejb</type>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>com.docdoku.plm</groupId>
            <artifactId>docdoku-plm-server-rest</artifactId>
            <version>${project.version}</version>
            <type>war</type>
            <scope>runtime</scope>
        </dependency>
    </dependencies>
</project>
